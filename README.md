# AayaamX Code Editor

This repository contains the full source code for the AayaamX web application.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   ```bash
   npm install
   ```
2. Set up environment variables in `.env.local` if using custom API integrations.
3. Run the development server:
   ```bash
   npm run dev
   ```

